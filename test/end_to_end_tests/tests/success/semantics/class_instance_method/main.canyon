fun main() {
    let foo: Foo = Foo::new();
    let result: i32 = foo.bar();
}

class Foo {
    let x: i32 = 0;
}

impl Foo {
    constructor new(self: Foo) {
        self.x = 1;
    }

    fun bar(self: Foo): i32 {
        self.x
    }
}